# Play Console「データ セーフティ」記入シート — sngnav-app

作成: 2026-08-10 · 対象コミット: `3a272a5` (main) · 対象ビルド: `flutter build appbundle --release` 2026-08-10 18:28

**再測定: 2026-09-25**（`5b80fcb` のコードと、この変更で直した User-Agent）。再測定したのは **§1 の 2 行目と §1-2 の通信の一覧、§2-1 の「送信されないもの」**。ほかの節のコード行番号の引用は 2026-08-10（`3a272a5`）のままで、再測定していない。⚑ 以前この行は、2026-09-24 以降の追記を含む本文全体を 2026-08-10 の測定として示していた。

> **これは何か。** Play Console の **App content → Data safety** フォームにオーナーが転記するための
> 回答シートです。フォームはオーナーの手で埋めるもの（外部への行為はすべてオーナーが行う）。ここにあるのは
> **回答そのものと、その根拠になったコードの行**です。
>
> **なぜ要るか。** データセーフティの申告は「だいたい合っている」では足りません。**申告と実装の食い違いは
> ポリシー違反**で、審査差し戻し・公開停止になります。10 月 31 日にテスターの手に渡すという一本道の上で、
> ここを落とすと日程ごと落ちます。だから憶測ゼロで、実際に走っている通信だけから書いています。
>
> **境界（正直に）。** ここに書いたのは**測ったコードの事実**です。事実を Play の分類語
> （Collected / Shared / ephemeral）にどう当てはめるかには**判断が要る箇所が 1 つ**あり、そこは
> 「作成者の推奨 + 争える余地」の形で残してあります。決めるのはオーナーです。

---

## 1. 前段の 3 問

| Play の質問 | 回答 | 根拠 |
|---|---|---|
| このアプリはユーザーデータを収集または共有しますか | **はい** | 位置情報が端末外に出る経路が 3 つある（§2） |
| 収集するユーザーデータはすべて転送時に暗号化されますか | **はい** | 下の §1-2 の **6 つの通信すべて** `https://` — アメダス観測 `jma_fetch.dart`、予報 `services/jma_forecast_fetch.dart`、警報・注意報 `advisory_service.dart`（`condition_aggregator_jma` / `noaa_nws_adapter`）、経路検索 `main.dart` の `_osrmDemoBaseUrl`、地図タイル `akita_map.dart`、**更新確認 `update_check.dart`**。⚑ 2026-09-25 訂正: この欄は「送信先 5 つ」として 5 ファイルを挙げ、予報を含めて**経路検索（OSRM）を落としていた**。プライバシーポリシーの一覧は逆に経路検索を含めて予報を落としていた。両方を §1-2 の同じ 6 つにそろえた。⚑ **根拠を grep から制約に変更（2026-09-25）。** 旧根拠は `grep -rn "http://" lib/`（2026-08-10）だったが、更新確認は配布先ホストを**取得した一覧の中から**受け取るため `lib/` に URL リテラルが無く、grep も `tool/assert_disclosure_parity.sh` の egress スキャナも**構造的に見えない**。そこで `UpdateManifest._httpUri` が `https` 以外を**拒否**するようにし、`test/services/update_check_test.dart` の陰性対照 2 件で「`http://` は拒否・同一 manifest の `https://` は通過」を固定した。この欄は走査結果ではなく**コード上の制約**に依拠する |
| ユーザーがデータの削除を要求する手段を提供していますか | **いいえ（ただし理由あり）** | アカウントもサーバーも存在しないため、削除すべき保管データが我々の側に無い。端末内のファイルはアンインストールで消える。フォームの自由記述に「アカウント・サーバーなし／端末内のみ／アンインストールで削除」と書く |


### 1-2. 端末の外に出る通信（プライバシーポリシーの一覧と同じ 6 つ）

この一覧は `privacy_policy_ja.md` の「端末の外に出るデータ」と**同じもの**で、同じ順に並べている。項目を足すときは両方を同じ変更で直すこと。`test/store/egress_inventory_parity_test.dart` が、両方の一覧（ポリシーは日本語と英語の両方）の項目と順番が一致することを確かめる。

1. **気象庁アメダス観測** — 秋田県内の固定 5 か所。座標を含まない。
   <!-- flow: jma-amedas -->
2. **気象庁の予報** — 秋田県（050000）固定。座標を含まない。
   <!-- flow: jma-forecast -->
3. **警報・注意報** — 共有中は日本＝都道府県コード、米国＝地点座標。共有していないとき、または位置がまだ分からないときは秋田県の分（座標を含まない）。
   <!-- flow: warnings -->
4. **経路検索** — タップした出発地・目的地の座標を OSRM へ（同意後のみ）。
   <!-- flow: route -->
5. **地図タイル** — 同梱範囲外を表示したときのタイル座標を OSM へ。
   <!-- flow: tiles -->
6. **更新確認** — 公開バージョン一覧（一覧が新しい https の置き場所を示したときは、それを確かめる 1 回の要求を含む。確かめられた置き場所は端末に保存し、次の起動から使う）と、新しいビルドがあるときの存在確認。座標・端末識別子を含まない。
   <!-- flow: update-check -->

このほか、アプリ自身の通信ではないものとして、OS の音声提供元（ネットワーク音声の設定時）と、OS の位置情報サービス（Google Play 開発者サービスがあれば Google の Fused Location Provider）がある。どの通信でも、通信先には端末の IP アドレスが届く。
---

## 2. データ種別ごとの回答

### 2-1. 位置情報 — Precise location（正確な位置情報）

| 項目 | 回答 |
|---|---|
| Collected（端末外に出るか） | **はい** |
| Shared（第三者に渡るか） | **はい**（作成者の推奨 — §3 参照） |
| Processed ephemerally（一時処理のみか） | **はい** — 座標はメモリ上のみ。端末にも保存しない (`main.dart:753-770` の経路、保存先なし) |
| Required / Optional | **Optional** — 同意しなければ送信は発生しない (`services/route_consent.dart`; 同意ゲートは deny-by-default) |
| 目的 | **App functionality**（アプリの機能） |

**端末外に出る 3 経路（これがすべて）:**

1. **警報・注意報の取得** — 現在地を共有しているあいだ（共有は同意のあとにだけ始まる）、走行約 1 km ごと（停車中・画面オフの運転中も約 10 分ごと）に、その地域を管轄する公的気象機関のみへ問い合わせる。**日本＝気象庁へは、端末内で判定した都道府県コードだけ（座標は送らない。都道府県単位のおおまかな位置 → 2-2 の Approximate にも当たる）。米国＝NWS へは地点座標（→ この 2-1 の Precise）。** 出典は記号で: `condition_aggregator_jma` 0.7.1 の `jma_advisory_provider.dart`（要求 URL `$warningJsonBaseUrl$prefectureCode.json`）、範囲判定 `services/provider_coverage.dart` / `advisory_service.dart` の `coversPoint`。⚑ **2026-09-25 訂正:** 旧記述は「日本でも現在座標を気象庁へ送信」だった。アプリ内開示（`AppL10n.locationDisclosure`）とコードの両方に反する。**Play への回答（Precise = Collected/Shared はい）は変わらない** — 米国の NWS 経路と下の経路検索が、正確な座標を端末外に出すため。
2. **経路検索** — ユーザーが**地図をタップして指定した**出発地・目的地の座標を OSRM 公開デモサーバー (`router.project-osrm.org`) へ。**GPS 現在地は経路検索へ自動送信されない。** `main.dart:868-885, 921-926, 956`
3. **地図タイルの補完** — 同梱オフライン地図の範囲外を表示したときのみ、タイル座標を `tile.openstreetmap.org` へ（→ 2-2 の Approximate 扱い）。`akita_map.dart:90` / `services/offline_basemap.dart:53-56`

**送信されないもの:** 気象庁アメダス観測値の取得は**固定の観測所 ID**（秋田県内 5 地点）に対する要求で、**ユーザーの座標を含まない**。気象庁の予報の取得も**秋田県（050000）固定**で、座標を含まない。どちらも共有するかどうかに関係なく、起動時から行われる。出典（記号）: `jma_fetch.dart` の `fetchLatestObservation` / `fetchCorridorObservations`、`services/jma_forecast_fetch.dart` の `fetchJmaForecast`。⚑ 2026-09-25: 予報はこの欄に無かった。

### 2-2. 位置情報 — Approximate location（おおよその位置情報）

| 項目 | 回答 |
|---|---|
| Collected | **はい** — 表示中のタイル座標は「おおよその現在地」に相当する情報 |
| Shared | **はい**（OpenStreetMap タイルサーバー = 第三者） |
| Processed ephemerally | **はい** |
| Required / Optional | **Required**（地図表示に伴う。ただし同梱範囲内は通信ゼロ＝オフライン） |
| 目的 | **App functionality** |

### 2-3. アプリのアクティビティ / 診断 — Crash logs, Diagnostics

| 項目 | 回答 |
|---|---|
| Collected | **いいえ** |

理由（Play の「ユーザー起点の転送」例外に該当）: エラーログは端末内ファイルにのみ追記され（上限 200 KB、古い順に破棄）、**自動送信は一切ない**。端末外に出るのは、ユーザーが「ログを共有」を押して OS の共有シートで自分の選んだ相手に送ったときだけ。`services/error_log.dart:9-15,32,54,83,96` / `services/log_share.dart:96,101`

同じ扱い: **運転日記**（Ring-2）— 端末内ファイルのみ、共有は「日記を共有」タップ時の OS 共有シートのみ。**座標・経路・目的地は一切記録しない**（記録されるのは彼女が自分で打った文だけ）。`services/drive_diary.dart:14-25,135,264,307`

### 2-4. その他すべての種別 — 収集なし

**Personal info / Financial / Health / Messages / Photos & videos / Audio / Files & docs / Calendar / Contacts / Web browsing / Device or other IDs → すべて「いいえ」。**

根拠: アカウント機能なし・ログインなし・個人情報入力欄なし・**広告 SDK / 解析 SDK ゼロ**・独自サーバーなし。要求している権限は INTERNET / ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION / WAKE_LOCK / VIBRATE / FOREGROUND_SERVICE / FOREGROUND_SERVICE_LOCATION / POST_NOTIFICATIONS の 8 つのみで（⚑ 2026-09-24 更新: 末尾 3 件は運転者自身が開始する通知つき前景サービスの着地に伴うもの。5 → 8。POST_NOTIFICATIONS は実機 API 34 での測定により追加 — これが無いと前景サービスの通知が黙って破棄され、運転者に何も見えないまま位置情報だけが使われる状態になった）、ストレージ・カメラ・連絡先・**バックグラウンド位置情報**は要求していない（配布マニフェスト `packaged_manifests/release/…/AndroidManifest.xml:15,21,22,27,45`（⚑ 行番号は 2026-08-24 に実ビルド成果物で再測定。旧記載の `:53` は現在 `<queries>` を指しており、主張は真・**引用先が壊れていた**。なお同マニフェストには 6 本目の `uses-permission` として `…DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`（`:75`、自己宣言・`protectionLevel="signature"`）が存在するが、AndroidX が自動生成する署名レベルの内部権限であり、ユーザーに提示されず Data safety の対象外。「8 つ」はユーザー向け `android.permission.*` の数） で確認）。

---

## 3. 判断が要る 1 点 — オーナーが決めること

**論点:** 座標が気象庁 / NWS / OSRM / OSM へ渡ることを、Play の言う **"Shared"（第三者への転送）** と申告するか。

- **狭く読む余地:** 経路検索はユーザーが目的地をタップした結果であり、「ユーザー起点の転送」例外に当たると主張できる。
- **作成者の推奨: 3 経路とも "Shared" として申告する（広い方）。**
  - 理由 1: 警報取得は走行中 1 km ごとの**継続的**送信で、「1 回のユーザー操作」とは形が違う。
  - 理由 2: **過少申告はポリシー違反（公開停止のリスク）だが、過大申告は違反にならない。** 10 月 31 日という戻ってこない日程の前で、非対称なリスクの安全側を取る。
  - 理由 3: 我々のプライバシーポリシーは既にこの 3 経路を明記しており、申告と一致する。
- **これは法的判断であり作成者の権限ではない。** オーナーが別の読みを採るなら、その読みでプライバシーポリシー側も揃える必要がある（両者の食い違い自体が指摘対象になる）。

---

## 4. 併せて必要な App content 項目（データセーフティ以外）

| 項目 | 状態 | 必要な行為 |
|---|---|---|
| プライバシーポリシー URL | 原文あり・**公開 URL は要 push** | `docs/store/privacy_policy_ja.md` は公開リポジトリにあり HTTP 200。⚑ **2026-08-24 再測定: 公開中の版は `origin/main` とバイト一致**（`raw.githubusercontent.com` GET、sha256 `59f6ebbc…5bde`）で、**VIBRATE 行は公開版に存在する**。訂正は `7339570` で landed 済み。この行は 2026-08-10 時点では正しく、**14 日間 stale のまま「push してから登録せよ」と upload 当日の作業を止めていた**。登録をブロックする条件は無い |
| 広告の有無 | **広告なし** | SDK ゼロ（上記） |
| コンテンツ レーティング アンケート | 未実施 | オーナーがフォーム記入（数分） |
| 対象ユーザー層 | 未設定 | 成人向け（運転者向けアプリ）。子ども向けではない |
| 位置情報の権限申告 | 前景のみ | バックグラウンド位置情報を要求していないため、Play の背景位置情報レビュー（重い方）は**不要**。⚑ 2026-09-24: 前景サービス（`foregroundServiceType="location"`）が実際に動くようになったが、`ACCESS_BACKGROUND_LOCATION` は依然として要求しておらず、この行の判定は変わらない |
| 前景サービスの申告 | ⚑ **未確認 — 提出前に要確認** | API 34 以降、`FOREGROUND_SERVICE_LOCATION` を宣言するアプリは Play Console 側でも前景サービスの用途申告を求められる可能性がある。2026-09-24 に本アプリはこの権限を宣言するようになったため、**この提出義務の有無は本欄の担当が Play Console の現行要件を実地で確認すること**。AAE はマニフェストを測れるがコンソール要件は測っていない — UNVERIFIED であって clear ではない |
| 政府アプリ / 金融機能 / 健康 | すべて該当なし | — |

---

## 5. このシートの正直な限界

- **これは記入シートであって、記入ではない。** フォームを埋めるのはオーナーの手。
- **コミット固定。** 上記は `3a272a5` の測定で、§1 の 2 行目・§1-2・§2-1 の「送信されないもの」だけが 2026-09-25 に `5b80fcb` で再測定されている。**新しい通信経路が 1 つでも増えたら、この表は嘘になる。** 送信先を足す変更をしたら、同じ変更でこのシートと `privacy_policy_ja.md` を更新すること。⚑ この注意書きは 2026-09-24 に一度、書かれたとおりに発火した（5 件目の通信が足され、どちらの文書も直されなかった）。2026-09-25 からは一覧の一致を `test/store/egress_inventory_parity_test.dart` が確かめる。ただしそのテストが確かめるのは**文書どうしの一致**であり、**コードとの一致ではない**（コードとホストの照合は `tool/assert_disclosure_parity.sh`。同じホスト上の別の通信は、どちらにも見えない）。
- **実機での確認は未実施。** 本シートはコードの読みと、この日ビルドした release 成果物のマニフェスト読みに基づく。実機で「同意ダイアログが出て、拒否したら送信が起きない」ことを見る確認は**別**（到達は端末で確かめる。端末が要る）。
